datasource db {
  provider = "sqlite"
  url      = "file:../../db.sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Product{
    id          String  @id @default(uuid())
    name        String 
    weight      Int
    brand       String?
    price       Int
    createdAt   DateTime   @default(now())
}